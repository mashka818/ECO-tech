# Временная заглушка для фронтенда
FROM nginx:alpine

# Копируем статические файлы
COPY . /usr/share/nginx/html

# Nginx уже настроен в основном контейнере
# Этот Dockerfile нужен для сборки образа фронтенда

